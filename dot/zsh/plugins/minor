function gci () {
  git commit -m "$*"
}

function kill_dev_processes() {
  pkill -9 ruby
  pkill -9 puma
  pkill -9 spring
  pkill -9 node
  kill -9 $(lsof -wni tcp:3000 -t)
  kill -9 $(lsof -wni tcp:5000 -t)
  kill -9 $(lsof -wni tcp:5000 -t)
  kill -9 $(lsof -wni tcp:5051 -t)
}
