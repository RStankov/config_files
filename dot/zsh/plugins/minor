function gci () {
  git commit -m "$*"
}

function kill_dev_processes() {
  npx kill-port 3000
  npx kill-port 3001
  npx kill-port 5000
  npx kill-port 5001
  npx kill-port 5051
  pkill -9 ruby
  pkill -9 puma
  pkill -9 spring
  pkill -9 node
  pkill -9 flow
}
